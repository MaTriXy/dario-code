---
phase: 02-hook-config-migration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/core/hooks.mjs
  - tests/hooks-migration.test.mjs
autonomous: true
requirements:
  - HOOK-06

must_haves:
  truths:
    - "Hook configuration is captured as a snapshot at session start"
    - "Running hooks uses the snapshot, not a fresh config read"
    - "When config files change mid-session, a warning is emitted"
    - "The snapshot persists for the session lifetime"
  artifacts:
    - path: "src/core/hooks.mjs"
      provides: "Snapshot creation, cached hook retrieval, change detection"
      exports: ["snapshotHooks", "getCachedHooks", "checkHookIntegrity"]
    - path: "tests/hooks-migration.test.mjs"
      provides: "Unit tests for HOOK-06 snapshot behavior"
      contains: "snapshot"
  key_links:
    - from: "src/core/hooks.mjs:snapshotHooks"
      to: "src/core/hooks.mjs:loadHooks"
      via: "Calls loadHooks once, stores result + hash"
      pattern: "snapshotHooks"
    - from: "src/core/hooks.mjs:runHooks"
      to: "src/core/hooks.mjs:getCachedHooks"
      via: "Uses cached snapshot instead of loadHooks on every call"
      pattern: "getCachedHooks"
---

<objective>
Implement hook session snapshot and change detection (HOOK-06). Hooks are captured once at session start and cached. Mid-session config changes trigger a warning but do not alter running hooks.

Purpose: Prevent mid-session config changes from causing unpredictable hook behavior. Match Claude Code's snapshot-then-warn approach.
Output: Snapshot/cache layer in hooks.mjs with change detection, plus tests.
</objective>

<execution_context>
@/Users/jkneen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jkneen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-hook-config-migration/02-RESEARCH.md
@.planning/phases/02-hook-config-migration/02-01-SUMMARY.md

@src/core/hooks.mjs

<interfaces>
<!-- From Plan 01 — new exports available after 02-01 completes -->
export function normalizeHookConfig(hookList) // Converts flat/nested to normalized format
export function deduplicateHandlers(handlers) // Dedup by type+command
export function clearOnceState() // Reset once tracking
export function loadHooks() // Returns normalized hooks from settings
<!-- Existing -->
export async function runHooks(hookType, context, verbose)
export async function runSessionStart(sessionId, context, verbose)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add snapshot tests and implement session snapshot with change detection</name>
  <files>src/core/hooks.mjs, tests/hooks-migration.test.mjs</files>
  <behavior>
    - snapshotHooks() calls loadHooks(), stores normalized result in module-level cache, computes SHA-256 hash of JSON.stringify(result)
    - getCachedHooks() returns the cached snapshot (not a fresh loadHooks call)
    - getCachedHooks() returns null if snapshotHooks() was never called (pre-snapshot state)
    - checkHookIntegrity() calls loadHooks() fresh, computes hash, compares to snapshot hash — returns true if same, false if changed
    - clearHookSnapshot() resets cache and hash to null (for tests and session end)
    - runHooks uses getCachedHooks() when snapshot exists, falls back to loadHooks() when no snapshot (backward compat for callers that don't snapshot)
    - When checkHookIntegrity() returns false, a warning message is available (not thrown, returned)
  </behavior>
  <action>
    1. Add tests to tests/hooks-migration.test.mjs in a new "HOOK-06: Session snapshot" describe block:
       - snapshotHooks captures config and getCachedHooks returns it
       - getCachedHooks returns null before snapshot
       - checkHookIntegrity returns true when config unchanged
       - checkHookIntegrity returns false when config changes mid-session
       - runHooks uses cached hooks when snapshot exists
       - clearHookSnapshot resets the cache

    2. Implement in src/core/hooks.mjs:
       - Import createHash from 'crypto'
       - Add module-level: let _hookCache = null, let _hookHash = null
       - snapshotHooks(): load + normalize all hooks, store in _hookCache, compute hash, store in _hookHash, return the cache
       - getCachedHooks(): return _hookCache
       - checkHookIntegrity(): if no snapshot return true; load fresh, hash, compare to _hookHash; return { changed: bool, warning: string|null }
       - clearHookSnapshot(): set _hookCache = null, _hookHash = null
       - Update runHooks: const allHooks = getCachedHooks() || loadHooksNormalized() (where loadHooksNormalized applies normalization)
       - Export snapshotHooks, getCachedHooks, checkHookIntegrity, clearHookSnapshot

    3. Update runSessionStart to call snapshotHooks() automatically so hooks are captured at session start without callers needing to know about snapshots.
  </action>
  <verify>
    <automated>npx vitest run tests/hooks-migration.test.mjs</automated>
  </verify>
  <done>
    - snapshotHooks captures and caches normalized hook config with hash
    - runHooks uses cache when available, loadHooks when not
    - checkHookIntegrity detects mid-session config changes
    - clearHookSnapshot resets for clean test isolation
    - All existing tests still pass (npm run test:unit)
  </done>
</task>

</tasks>

<verification>
npx vitest run tests/hooks-migration.test.mjs
npm run test:unit
</verification>

<success_criteria>
- Snapshot tests pass covering all HOOK-06 behaviors
- runHooks no longer calls loadSettings on every invocation when snapshot exists
- checkHookIntegrity correctly detects config changes via hash comparison
- Full test suite green — no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-hook-config-migration/02-02-SUMMARY.md`
</output>
